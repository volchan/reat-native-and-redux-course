import React from 'react';
import {Text, TouchableOpacity} from 'react-native';

const Button = () => {
  return (
    <TouchableOpacity>
      <Text>Click Me!!!!</Text>
    </TouchableOpacity>
  );
};

const styles = {};

export default Button;
